/// <reference types="expo/types" />

// Provides ambient TypeScript types for Expo (CSS/asset module imports, env, etc.).
// Committed (not gitignored) so `tsc` passes locally and in CI without a prior
// `expo` command. Expo may regenerate this file with identical content.
